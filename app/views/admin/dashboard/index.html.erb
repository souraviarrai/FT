<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Section</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

  <!-- Optional: Add your custom CSS here -->
</head>

<body>
<div class="container">
  <h1>Hello, welcome to the admin section</h1>
  <%= button_to 'Sign Out', destroy_user_session_path, method: :delete %>
  <h1>Stock, Present right now</h1>

  <!-- Bootstrap table -->
  <table class="table table-bordered">
    <thead>
    <tr>
      <th>Ticker</th>
      <th>Name</th>
    </tr>
    </thead>
    <tbody>
    <% @stocks.each do |stock| %>
      <tr>
        <td><a href="/admin/dashboard/<%= stock.id %>"><%= stock.ticker %></a></td>
        <td><%= stock.name %></td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <%= link_to 'Add Stock', new_admin_dashboard_path %>
</div>

<!-- Bootstrap JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<!-- Optional: Add your custom JavaScript here -->
</body>

</html>
